# Plan

This file is the active todo list and immediate status for the project.

## Rules

- Keep this file focused on current todos, status, and next actions.
- Update it before starting a new implementation phase.
- Move detailed ticket context into `contexts/context.md` or `contexts/tickets/`.
- Follow `AGENTS.md` for Codex operating rules.
- Read `contexts/index.md` before major planning or implementation work.
- Keep durable knowledge in `contexts/wiki/`.
- Keep raw notes in `contexts/inbox.md`.
- Give each subagent one ticket from `contexts/tickets/`.
- Put subagent reports in `contexts/handoffs/` before curation.

## Current Status

- Template installed.
- Project-specific context has not been filled in yet.

## Todo

- [ ] Add base project idea to `contexts/inbox.md`.
- [ ] Normalize base idea into `contexts/wiki/project-overview.md`.
- [ ] Create first project tickets under `contexts/tickets/`.
- [ ] Record first durable decisions in `contexts/decisions.md`.
- [ ] Create runbook/QA/post-deploy ops artifacts only after runnable code exists and the user explicitly requests it.
